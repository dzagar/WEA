
import { inlineCompare } from 'students-records/helpers/inline-compare';
import { module, test } from 'qunit';

module('Unit | Helper | inline compare');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = inlineCompare([42]);
  assert.ok(result);
});

