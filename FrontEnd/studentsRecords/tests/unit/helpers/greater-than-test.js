
import { greaterThan } from 'students-records/helpers/greater-than';
import { module, test } from 'qunit';

module('Unit | Helper | greater than');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = greaterThan([42]);
  assert.ok(result);
});

