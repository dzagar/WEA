
import { inlineMath } from 'students-records/helpers/inline-math';
import { module, test } from 'qunit';

module('Unit | Helper | inline math');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = inlineMath([42]);
  assert.ok(result);
});

